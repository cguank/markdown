### 895.最大频率栈
- maxFreq
- valToFreqMap
- freqToValListMap
### 146.LRU
- 双端link
- map
### 460.LFU
3个表
- keyToValMap
- keyToFreqMap
- freqToKeysSetMap
- minFreq
### 1670.前中后队列
- 两个双端队列，leftList和rightList
### 1514 DJS算法
点对点最短路径
如何处理负权重？
- 优先级队列
- 贪心思想
- BFS
- djsToList记录到达每个结点的最小值
- 如果有边的限制，stepList记录到达每个点的最少边
### 1334 Floyd算法
多源最短路径
- 3重for循环，第一层为中间结点
- dp[i][j]=Math.min(dp[i][j],dp[i][k]+dp[k][j])
### 1584 Kruscal最小生成树
- Union并查集
- 链接所有顶点的权重最小
## 494 01背包
```
每个物品只能选择一次
3个物品，总重量为4，在重量<=4的情况下使得选取的价值和最大
N = 3, W = 4
wt = [2, 1, 3]
val = [4, 2, 3]
```
- 每个物品只能选一次，因此第一个for循环为物品
- 每个物品只有选或者不选
- dp\[i\]\[w\]表示前i个物品中，不超过w的情况下所能选择的最大价值和（思考下为何      d\[w\]不行）

## 518完全背包
- dp\[i\]\[j\]表示金额为i下，选择前j个硬币得出的组合数
- 状态方程
```js
// 选择当前硬币
dp[i][j]=dp[i][j-1] + dp[i-coin[j]][j]
// 不选择当前硬币
dp[i][j]=dp[i][j-1]
```

### 1094差分数组
```js
increament(i,j,v) {
	this.diffs[i]+=v
	if(j+1<this.diffs.length) {
		this.diffs[j+1]-=v;
	}
}
getResult () {
	const res = [this.diffs[0]];
	for (let i = 1; i < this.n; i++){
		res[i] = res[i - 1] + this.diffs[i];
	}
	return res;
  }
```
### 优先队列
最大堆和最小堆
### 514轮盘问题
- 用map存储每个char的索引
- 可以正着转也可以反着转，就是索引相减的绝对值，反着转就是长度减去绝对值
### 反转思路
- 174 这一题看着和最小路径和很像，但是从左往右是做不出来的；得从右往左、下往上走；
- 213: 这一题不要光想着吧首尾相连，应该取只包含首和只包含尾的最大值